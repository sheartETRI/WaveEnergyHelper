# REPORT_OVERSOLD_STUDY — 12차: 조건부 과매도 forward 분포 연구

작성일: 2026-07-10 / 발주: 김박사 / 실행: Claude Code
성격: **조건부 분포 연구.** 시그널 검증 아님. 김박사 확정 — "결과가 좋아도 여기까지."
forward 등록·필터화·매매 시뮬레이션으로 이어지지 않는다. PHASE7 종료 결정은 유효.

---

## 연구 질문 (김박사 가설, 데이터 확인 전 제시 — 사전 등록 자격)

과매도 매수는 승률이 높은 편이나 무조건은 아니다.
- **① C1** 완전 역배열의 과매도는 오히려 나쁘다 (회피 가설).
- **② C2** 장기 이평선(120일선) 상승 중의 과매도는 좋은 기회다.
- **③ C3** 상위 프레임(4d) MACD 골든크로스 이후의 과매도는 확률이 높다.

## A. 지표·이벤트 정의 (동결)

- **과매도 = Wilder RSI(14) ≤ 30** (김박사 확정). 신규 지표 `indicators/rsi_wilder.py`
  (`WILDER_RSI_PERIOD=14`, `OVERSOLD_THRESHOLD=30.0` 노출). 유효 구간에서 기존
  `oscillators.add_rsi`의 `rsi_raw`와 비트-동일(테스트), 워밍업은 NaN(경계 오검출 방지).
- **이벤트 = RSI가 30을 상→하 교차한 봉** (직전>30 & 현재≤30, 진입 봉만 — 연속 과매도 중복 방지).
- **대상 = BTC/ETH/BNB/SOL 일봉.** 심볼당 이벤트가 희소(전체 21~36건)하여 **4심볼을 풀링**한다
  (위임 "다중비교 억제" — 심볼별 개별 검정 대신 단일 풀 분포; n≥30 임계는 풀링을 전제로 성립).
- **forward = 익일 시가 진입 → k일 뒤 시가 청산 로그수익** (8차 `regime_forward`와 통일).
  주 창 k=20, 부가 5·60. 범위 밖(청산 봉 없음) 이벤트는 제외.
- **겹침**: 이벤트 forward 창 중복은 그대로 두고, n과 함께 **유효표본(비중복 근사)**을 병기한다
  (심볼별 시간순 그리디로 20봉 이상 간격 이벤트만 세는 근사).
- **조건군**:
  - **C0** 무조건 (베이스라인).
  - **C1** 완전 역배열: `MA10 < MA20 < MA60 < MA120` (이벤트 봉).
  - **C2** 장기 상승: `sign(MA120[t] − MA120[t−5]) > 0` (기법0 slope 정의·N=`TREND_SLOPE_N`=5).
  - **C3** 상위 TF MACD GC 국면: 4d(1d의 상위) MACD선 > 시그널선. 4d는 1d를 리샘플
    (app과 동일 파라미터: `4D`, label/closed=right, origin=start), MACD(12,26,9)를 4d 종가에
    산출 후 봉 마감(우측 라벨) 상태를 1d로 as-of ffill(선견 없음).
  - 교차 **C2∩C3** 포함(탐색 단계 조합 허용).

## 홀드아웃 설계 (탐색/확인 시간 분리 — 이 프로젝트 최초)

- **심볼별 시간 50:50 분할**: `mid = n // 2` (봉 수 기준 중앙). 전반=탐색, 후반=확인.
- **후반 데이터는 탐색 단계에서 로드 금지 (코드 수준 분리)**:
  - `explore` 실행은 `full.iloc[:mid]`만 로드해 지표·이벤트·forward를 전반 안에서 계산한다
    (전반 이벤트의 forward 창은 전반 안에서만 — split 이전 시가만 사용).
  - `confirm` 실행은 full을 로드하되(지표 워밍업 연속성) `idx ≥ mid` 이벤트만 채점하고 forward는
    후반 시가만 사용한다. 지표는 인과적(백워드)이라 전반 구간 값은 두 로드 방식에서 동일하다.
  - 코드 불변식 assert: explore 이벤트·forward는 split_ts 이전, confirm은 split_ts 이후.
- **선정과 확인 기준은 후반 개봉 전 본 리포트에 기록**(아래 선정 규칙 → §B 선정 → 커밋 → 그 후 `confirm`).

### 분할 메타 (심볼별, `explore` 실행 기록)

| 심볼 | 전체 봉 | split 인덱스 | split 날짜 | 기간 |
|---|---|---|---|---|
| BTCUSDT | 3249 | 1624 | 2022-01-27 | 2017-08-17 ~ 2026-07-09 |
| ETHUSDT | 3249 | 1624 | 2022-01-27 | 2017-08-17 ~ 2026-07-09 |
| BNBUSDT | 3168 | 1584 | 2022-03-09 | 2017-11-06 ~ 2026-07-09 |
| SOLUSDT | 2159 | 1079 | 2023-07-26 | 2020-08-11 ~ 2026-07-09 |

## 선정 규칙 (사전 등록 — 탐색 수치 확인 전 고정)

> 아래 규칙은 전반(탐색) 수치를 보기 **전에** 확정한다. 규칙을 데이터에 맞춰 바꾸지 않는다.

- **양(+) 가설 후보 = {C2, C3, C2∩C3}.** 이 중 전반(풀) 기준으로 **① n ≥ 30 이고 ② fwd20 mean과
  median이 모두 C0(전반)보다 높은** 군을 자격 후보로 한다.
- 자격 후보를 **fwd20 mean의 C0 대비 상승폭** 내림차순으로 정렬해 **상위 최대 2개**를 확인 대상으로 선정.
  자격 후보가 2개 미만이면 자격을 갖춘 수만큼만(0~1개) 선정한다(억지로 채우지 않음).
- **C1(역배열)은 선정과 무관하게 항상 확인 대상**(회피 가설, 별도 기준으로 후반 단발 검정).
- 선정 결과·근거·타임스탬프를 §B에 기록한 뒤에만 후반을 개봉한다.

## B. 탐색 (전반 데이터만)

풀 이벤트 **58건** (BTC 16 · ETH 13 · BNB 14 · SOL 15). `oversold_explore.json` 산출.

| 군 | n | 유효n | fwd20 mean | fwd20 median | 승률(>0) | fwd5 mean | fwd60 mean |
|---|---|---|---|---|---|---|---|
| **C0** (무조건) | 58 | 41 | **+3.59%** | +0.71% | 51.7% | +2.24% | +11.91% |
| **C1** (역배열) | 31 | 23 | −0.90% | −1.18% | 45.2% | −3.60% | +8.56% |
| **C2** (120MA 상승) | 14 | 10 | −2.10% | −1.69% | 42.9% | +3.03% | −13.64% |
| **C3** (4d MACD GC) | 15 | 12 | +1.00% | −5.11% | 33.3% | −1.29% | +21.22% |
| **C2∩C3** | 1 | 1 | +5.15% | +5.15% | 100% | +3.19% | −10.30% |

관찰(탐색): C0(무조건 과매도)의 fwd20 mean은 +3.59%·승률 51.7%로 양(+) — "과매도 매수 승률이
높은 편"이라는 김박사 전제와 부합. 그러나 **양 가설 조건 C2·C3는 C0를 개선하지 못한다**:
C2(장기 상승 중 과매도)는 오히려 열위(−2.10% < +3.59%), C3(상위 MACD GC 이후)도 mean 열위(+1.00%)이며
median은 −5.11%로 강한 음(+큰 우편향, n=15). C2∩C3는 n=1로 무의미. 표본이 극히 희소(조건군 n=14~15)해
추정 분산이 크다.

### 선정 결과 (2026-07-10, 후반 개봉 전 기록)

**사전 등록 선정 규칙 적용** — 양 가설 후보 {C2, C3, C2∩C3} 중 전반 n≥30 이고 fwd20 mean·median이
모두 C0(전반, +3.59%/+0.71%)보다 높은 군:
- C2: n=14(<30) **및** mean −2.10%·median −1.69% 모두 C0 미달 → 자격 없음.
- C3: n=15(<30) **및** mean +1.00%·median −5.11% 모두 C0 미달 → 자격 없음.
- C2∩C3: n=1(<30) → 자격 없음.

→ **선정된 양(+) 조건: 없음 (0개).** 규칙상 억지로 채우지 않는다. 탐색 데이터가 가설 ②·③을
in-sample에서조차 지지하지 않는다(둘 다 C0 열위). **C1(역배열)은 규칙에 따라 회피 가설로 후반 단발
확인에 그대로 이월.** 후반에서 C0(베이스라인)와 C1만 채점하며, 미선정 조건(C2·C3)을 후반에서 추가로
열지 않는다(홀드아웃 다중비교 보존, 위임 금지사항 준수).

`oversold_selection.json`: `selected_conditions = []` (C1은 엔진이 자동 이월).

## C. 확인 (후반 데이터, 단발)

### 확인 기준 (사전 등록 — 결과 확인 후 변경 금지, 위임 원문 인용)

> - 선정 조건군: 후반에서 **n ≥ 30** 이고 fwd20 **mean·median 모두** C0(후반) 대비 **우위** →
>   "가설 지지"로 기록.
> - C1(역배열): 후반에서 **n ≥ 20** 이고 mean·median 모두 C0 대비 **열위** → "회피 가설 지지".
> - 미달은 미달로 그대로 기록 (각색·재탐색·기준 완화 금지. 후반에서 다른 조합을 추가로 열어보는 것 금지).

_(confirm 실행 후 표 + 판정 기입)_

## D. 고찰·한계·종결 — [confirm 후 기입]

**한계 문구 (위임 원문 포함)**:
> 본 결과는 조건부 분포에 대한 것이며 수수료·진입규칙·사이징을 포함한 매매 엣지가 아니다.
> 김박사 확정에 따라 본 연구는 forward 등록·필터화로 이어지지 않는다.

## 커밋 해시 — [최종 기입]
