"""시각 표기의 시간대 라벨 — 표시 계층 공용 상수 (변환 없음, 표기만).

프레임 인덱스는 바이낸스 open_time 을 naive 로 둔 UTC 이고, LW 차트도 UTC 로 그린다. 차트 캡션과 알람 탭
(마지막 봉·신호 이력 표 시각 헤더)이 같은 라벨을 쓰도록 한 곳에 둔다. 시간대 변환은 하지 않는다.
"""
UTC_LABEL = "(UTC)"
