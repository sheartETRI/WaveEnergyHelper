# INSTRUCTION_2027_03_REVIEW — 6개월 열람 지시문 (2026-09-04 동결)

이 문서는 2027-03-01 이후 실행할 열람 지시문을 데이터가 쌓이기 전에 동결한
것이다. 열람 시점에 이 문서를 수정한 뒤 실행하는 것을 금지한다 — 수정이
필요하면 수정 사실과 사유를 보고서 §0에 명기한다.

## 실행 조건
- 실행일 ≥ 2027-03-01. 조기 실행 금지 (헌장·섀도 미니 헌장의 열람 시점).

## A. F2-b 전방 보고 (판정 있음 — 결정표는 헌장에 이미 동결됨)
1. --annotate → --report 실행. 주 지표 = 전방 Δ′ (PAIR_B + PAIR_C 통합,
   승격과 동일), 월 클러스터 부트스트랩 CI.
2. 결정표 기계 적용: CI 상한 < 0 → RETRACT_REVIEW / Δ′<0 & CI가 0 포함 →
   EXTEND / 그 외 → KEEP / CI 산출 불가 → EXTEND. 표를 다시 고르지 말 것.
3. PAIR_C 셀 공백 여부 확인 — 공백·희소 시 판정은 통합 CI로만, 한계 기재
   (헌장 §2-1).
4. 커밋 감사표: 감사 기준 커밋(1906f76) 이후 정의 파일 7개 + integrity 테스트
   파일의 전체 커밋 목록과 각각의 사유. 감사표에 미승인 변경이 있으면
   판정보다 먼저 보고.

## B. 이벤트 키 패리티 — 추적 창 전 구간 재확인
1. 추적 창(2026-09-04 이후)에 대해 백테스트식 저널을 재생성.
2. 라이브 전방 저널과 이벤트 키 (symbol, timestamp, rule) 단위로 대조.
   봉 키 비교 금지 — 2026-09-04 하네스 버그(같은 봉 다중 rule 덮어쓰기)의
   재발 방지. 당시 만든 회귀 픽스처 통과를 먼저 확인.
3. 일치율과 불일치 목록 보고. 불일치가 있으면 A의 판정 신뢰도에 대한 영향을
   별도 절로 서술 (판정 자체는 그대로 기록).

## C. MM 섀도 1차 열람 (판정 없음 — 미니 헌장 §2의 항목만)
1. 3변형(BASE/NOSTOP/STRUCT)의 G·트레이드 수·손절 발동률.
2. STRUCT vs BASE 부호, 표본이 허용하면 월 클러스터 CI.
3. 위 항목 외 지표 추가 금지. 이 절에서 어떤 규칙 권고도 하지 않는다 —
   전부 참고 자료로 표기.
4. STRUCT의 백테스트 결과(Δ=+0.105, 격차 해소)와 부호가 일치하는지만
   사실로 병기.

## D. 보고서 형식
§0 이 지시문의 수정 여부 → §1 A 판정 → §2 B 패리티 → §3 C 섀도 →
§4 감사표 → §5 한계(표본 기간·국면 편중·PAIR_C 공백 등 실측 기반으로).

## E. 열람 후
- 다음 백테스트 라운드(분할 등)의 착수 여부와 가설은 이 보고를 근거로
  상위 결정한다. 이 문서는 그 결정을 미리 정하지 않는다.
